# Generated from actual imports across the repo (root pipelines, analytics/,
# tests/), pinned to versions installed in the working anaconda environment
# (C:\ProgramData\anaconda3) where a version could be resolved. Unpinned
# entries were not found installed anywhere in that env — install and pin
# them yourself before relying on the pipeline that needs them.

# ── Core (query layer, curated dedup, validation, most pipelines) ──────────
pandas==3.0.3
numpy==1.26.4
pyarrow==24.0.0
duckdb==1.5.4
requests==2.31.0
curl_cffi==0.15.0            # jmic_pipeline, schwab_holdings_pipeline (Cloudflare TLS-fingerprint bypass)
python-dotenv==1.2.2
scipy==1.10.1

# ── Test suite ──────────────────────────────────────────────────────────────
pytest==9.1.1

# ── Pipeline-specific (only needed by the pipeline(s) noted) ────────────────
yfinance==1.5.1              # price_history_pipeline, yfinance_pipeline, futures_pipeline, short_interest_pipeline, sector_etf_pipeline (fallback)
schwabdev==3.0.5              # schwab_*_pipeline.py (Stage 2, interactive OAuth)
beautifulsoup4==4.15.0        # fed_sentiment_pipeline, fund_holdings_pipeline (HTML/XML scraping)
lxml==6.1.1                   # bs4 parser backend
openpyxl==3.1.5               # usgs_minerals_pipeline, usgs_ds140_pipeline (Excel parsing)
pyiceberg==0.11.1             # index_constituents_pipeline, fund_holdings_pipeline, openfigi_pipeline, securities_reference_pipeline, create_securities_table
edgartools==5.35.0            # fund_holdings_pipeline (N-PORT mutual fund holdings)
vaderSentiment==3.3.2         # news_sentiment_pipeline
websockets==16.0              # ais_pipeline (AISStream.io WebSocket feed)
anthropic==0.116.0            # fed_sentiment_pipeline
huggingface_hub==0.35.0       # upload_huggingface.py
datasets==2.12.0              # upload_huggingface.py
transformers==4.56.2          # finbert_eval.py
torch==2.8.0                  # finbert_eval.py
plotly==5.9.0                 # generate_tv_rating_report.py
dash==4.4.1                   # backtest_app.py (live interactive explorer)
comm>=0.2.0                   # dash transitive dep -- <0.2.0 raises NotImplementedError on import outside Jupyter

# ── Pipeline-specific, not currently installed (pin after installing) ──────
pytrends       # google_trends_pipeline.py
praw           # reddit_pipeline.py
cot_reports    # futures_pipeline.py (CFTC Commitments of Traders)
