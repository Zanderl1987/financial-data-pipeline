# Tasks

- [ ] Verify HF config rebuild finished: datasets-server `/splits` should report `ready=152 / pending=0` (README 152-config YAML uploaded 2026-08-02; per-config rebuild STILL busy-500 after ~90+ min of background polling as of last check — normal mid-process state, don't call done; convert branch appears only at completion; note: etf_holdings parquet + README re-uploaded after the merge (row total now 96,981,484) — README text changed but configs block unchanged, so the rebuild should be unaffected)
- [x] Push `etf_holdings` to HF: full 119-fund universe run (7,539 rows, 08-02 snapshot) validated PASS 0/0, curated 0 dupes, then MERGED with existing HF history (07-30: 7,524 + 07-31: 199) → 15,262 rows / 3 snapshots / 119 funds, uploaded via `upload_file` (replacing old 7,723-row file); README row total 96,973,945 → 96,981,484 re-uploaded; CATALOG now 236 tables
- [x] Update CLAUDE.md: table count 133→235 (verified `len(query.CATALOG)` = 235) and test count 273→446 (full suite 446 passed / 18 skipped, 2026-08-02, after etf_holdings wiring)
- [x] Fix CLAUDE.md python path + repo root: `C:\ProgramData\anaconda3\python.exe` (gone) and `C:\Users\zande\PycharmProjects\...` (gone) → `C:\Users\Zander\AppData\Local\Programs\Python\Python312\python.exe` and `C:\Users\Zander\financial-data-pipeline` (both verified on disk); bare `python` now resolves to Python312, not a broken MS Store stub
- [x] Add category comment headers to the uncommented CATALOG block in query.py (fred_macro/fred_rates_gdp/alpha_vantage/coingecko/sec_edgar/bls/eia/finnhub/tiingo/treasury "extended" groups) — lake_manifest.py's prefix-fallback is now dead path; parser confirms 0 "unlabeled in source" entries across all 235 tables
- [x] Investigate `etf_holdings` on HF with no matching CATALOG entry — resolved: it's a one-off snapshot from SecuritiesDB's free keyless API (top-100 holdings/ETF with Piotroski/Altman Z/market-cap/sector). Was never wired because `upload_huggingface.py`'s `upload_folder` sweeps `storage/curated/**` and never deletes. Built `etf_holdings_pipeline.py` (119-fund universe, embedded name map, retry/backoff) and wired it fully: query.py CATALOG, validate.py SCHEMAS, run_all.py PipelineSpec (stage 1), curated.py KEYS, tests (both lists), docs/PIPELINE_CATALOG.md. Verified live (210 rows/3 funds test pull), validate PASS, curated 0 dupes, full suite 446 passed/18 skipped. Only remaining oddity: HF configs YAML already lists etf_holdings (it was on HF before wiring)
- [x] Add missing deps to `requirements.txt`: `sqlalchemy==2.0.51` (pyiceberg sql-sqlite extra) + `xlrd==2.0.2` (GSCPI Excel parsing), both verified installed in Python312
- [ ] Iceberg catalog stale-path bug recurred: `constituents_catalog.db` on the Passport-drive `storage/` still pointed at `C:\Users\zande\PycharmProjects\...` (the same bug SESSION_NOTES_2026-07-18-constituents.md already fixed once). Fixed `constituents.index_members`, `shipping.gscpi`, `shipping.freight_ppi` (dropped + recreated, confirmed empty locally so no data lost) — `constituents.securities`, `constituents.fund_holdings`, `constituents.identifier_map` still have the same stale pointer, not fixed (not needed for this session's work; HF already has current data for those 3)
- [ ] **`upload_huggingface.py`'s `upload_folder` call is unsafe to run as-is from this Passport-drive checkout.** Local `storage/curated/` is a partial subset (708MB vs. the full multi-GB published dataset) — a blind run would have silently replaced 9+ HF tables with much smaller local versions, including `fundamentals_quarterly` (would drop to 0.4% of published size) and `fundamentals_annual` (0.8%). Worked around this session via targeted `upload_file()` calls on only the 4 verified tables; the script itself needs either a size-sanity guard before upload or `delete_patterns`-aware logic, or this checkout needs a full local backfill before `upload_folder` is safe to run again.
- [x] Build scripts/lake_manifest.py (HF-backed data lake manifest exporter)
- [x] Build + publish interactive data lake dashboard artifact
- [x] Add negative-price guard to `curated.py` for the `prices` table (Schwab additive dividend-adjustment artifact) — nulls open/high/low/close/pct_change/log_return/intraday_change/intraday_range/vwap per-symbol through their last OHLC<=0 date; applied one-time to the published HF `prices` table (896,583 of 46,950,543 rows affected, 0 remaining after fix) and wired into `curated.py` for future recompactions
- [x] Refresh constituents on HF: ran `index_constituents_pipeline.py` fresh (7,587 rows, 2026-08-01 snapshot), merged with existing HF history (now 3 snapshots: 07-18/07-23/08-01) rather than overwriting it, pushed
- [x] Refresh shipping/freight on HF: `shipping_gscpi` confirmed already complete (342 rows through 2026-06, full-history pipeline); `shipping_freight_ppi` incremental pull merged with existing HF history (5,282 rows preserved), pushed
- [x] Push last 2 local-only tables to HF (`cfpb_complaints`, `open_meteo_weather`) → 152 tables / 96,973,945 rows on HF
- [x] Fix HF viewer one-config problem: added 152-entry `configs:` YAML to README front matter + fixed `load_dataset(repo, "<table>")` usage example + counts (background per-config rebuild still running)
- [x] Research new data sources: Redfin (live S3 TSV.gz) + AQR factor library (live xlsx) buildable; IEA EV battery (403-gated), MSRB EMMA (paid), FINRA TRACE (registration-gated) skipped
- [x] Build `redfin_pipeline.py` (national/metro/state market tracker; 624K rows) + wire query.py/validate.py/run_all.py/curated.py/tests/docs
- [x] Build `aqr_factors_pipeline.py` (VME/QMJ/TSMOM monthly factors; 27K rows; TSMOM unlabeled-date-column parse fix) + wire all layers
- [x] Add Redfin + AQR rows to docs/PIPELINE_CATALOG.md; full suite 444 passed / 18 skipped
